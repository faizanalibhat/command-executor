const errorHandler = async (error, req, res) => {
  // if (err) {
  //   return res.json({ success: "false", error: err });
  // }
  // else {
  //   return res.json({message: "No Response By Controller..."});
  // }
}

module.exports = errorHandler;